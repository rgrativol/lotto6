use std::collections::{HashMap, LinkedList};

pub struct FrequencyStack{
    frequency_map: HashMap<u8,u32>,
    stack_map: HashMap<u8 , LinkedList<u32>>
}

impl FrequencyStack{
    fn push(&self){

    }
    fn pop(&self){

    }
}